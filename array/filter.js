const words = ["agus", "aren", "ari sandi", "awaludin", "arie akbar"];
const result = words.filter((word) => word.length > 6);
console.log(result);


const word = ["agus", "aren", "ari sandi", "awaludin", "arie akbar"];
const resultt = word.filter((wordd) => wordd.includes("r"));
console.log(resultt);
